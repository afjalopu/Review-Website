import React from 'react';
import image from './maxresdefault (1).jpg'

const NotFound = () => {
    return (
        <div>
            <img src={image} />
        </div>
    );
};

export default NotFound;